import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "orange"
    },
    secondary :{
      main: "white"
    },
    customColor: {
        main: "black"
    }
  },
});

export default theme;